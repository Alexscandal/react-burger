/* reducer*/
/*
import { FAILED_LOADING, LOAD_DATA } from '@/services/actions/ingradients.js';
import { getIngredients } from '@utils/api.js';
*/
const ingradientsInitialState = {
	items: [],
};

export const ingradientsReducer = (
	state = ingradientsInitialState /*, action*/
) => {
	return state;
};
